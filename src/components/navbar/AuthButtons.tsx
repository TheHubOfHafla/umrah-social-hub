
import { useNavigate } from "react-router-dom";
import { Button } from "@/components/ui/button";

// This component no longer renders any buttons since login is disabled
const AuthButtons = () => {
  return null;
};

export default AuthButtons;
